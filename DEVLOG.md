# DEVLOG — decisions & milestones

Decision-focused, timestamped. Appended via `scripts/log.sh "<decision>"`.
Distilled into the README summary in the final ~10 minutes.

<!-- entries appear below -->
- **[2026-07-22 12:17]** Voice path chosen: expo-audio record (LINEARPCM .wav 16k mono) -> base64 -> /api/transcribe -> OpenRouter input_audio w/ google/gemini-2.5-flash. Verified against SDK 54 + OpenRouter docs: expo-audio IS in Expo Go, OpenRouter DOES accept base64 audio. No dev build needed; Expo Go delivery preserved. Rejected RecordingPresets.HIGH_QUALITY (.m4a AAC) — OpenRouter warns 'not all models support all formats' and a format mismatch is indistinguishable from a bad transcription; .wav is the format in their own example.
- **[2026-07-22 12:17]** Rich text x diff tension flagged to Tim before building: diffing HTML yields garbage hunks. Decision (Tim): markdown is the document source of truth; LLM reads/returns markdown, diff runs on markdown, webview editor is a rich-text view over it.
- **[2026-07-22 12:17]** Built scripts/dev-api.mjs — local stand-in for the Vercel functions so the phone hits a real backend with no deploy per change. Imports the SAME api/*.ts handlers (node --experimental-strip-types) rather than reimplementing them, so there is no second copy to drift.
- **[2026-07-22 12:17]** BLOCKED: OPENROUTER_API_KEY in .env.local is 9 chars ('sk-or-' + 3). Diagnosed rather than guessed: a well-formed-but-invalid key returns 'User not found.', the supplied one returns 'Missing Authentication header' — OpenRouter rejects it as a credential outright. Request shape is proven correct; only the secret is wrong.
- **[2026-07-22 12:19]** Renamed Sotto -> Emend: app.json (name/slug/scheme + ios.bundleIdentifier com.timyang.emend), package.json, mic-permission copy, README title. Local only, no git-remote changes.
- **[2026-07-22 12:19]** Art direction recorded in src/theme/emend.ts (spring-clear light Regency: ivory #FBF7F0 / ink #2B2620 / champagne #C9A66B, diff on rose #F6DCD8 + sage #DDE7D2, Fraunces prose + Figtree chrome). Deliberately NOT wired in — the slice runs on the existing dark tokens so we are not debugging layout and voice at once. Polish pass re-exports it from tokens.ts and the whole UI kit turns over in one move.
- **[2026-07-22 12:22]** VOICE SPIKE STEP 1 GREEN: OpenRouter google/gemini-2.5-flash transcribed a 16kHz mono wav verbatim, 200 in 1294ms. Audio-in path confirmed with the real key. Expo Go delivery preserved — no dev build needed.
- **[2026-07-22 12:27]** VOICE SPIKE STEP 2 GREEN: api/transcribe.ts (the real Vercel handler, served locally by dev-api) transcribed the wav verbatim over the LAN address the phone uses — 200 in 1.36s. Client leg proven up to the microphone itself.
- **[2026-07-22 12:27]** Bundle audit before handing the phone a QR: pulled the actual dev bundle Expo Go loads (/.expo/.virtual-metro-entry.bundle, 10MB) and grepped it. Voice screen present, expo-audio linked (24 refs), endpoint inlined, ZERO occurrences of sk-or-v1 — the OpenRouter key is not in the client bundle. First bundle URL I tried (expo-router/entry) returned a partial 6MB graph missing all app strings; that would have been a misleading green. Checked the right artifact instead of trusting the first 200.
- **[2026-07-22 12:28]** App icon landed: v2 of three AI-generated iterations Tim art-directed (serif E in champagne gold on ivory, blush-rose bloom + sage leaves laid across the upper arm — reads as a pen against the letterform). Installed to assets/images/icon.png (1024x1024, no alpha); all three copied to docs/icons for the README. Shell brand gate now passes — it had been correctly refusing the rig placeholder.
- **[2026-07-22 12:46]** Seed document replaced with James's exact passage (verbatim, 3 paragraphs) in src/lib/api/sample.ts. Kept it there rather than the suggested src/lib/doc/seed.ts because src/lib/doc/ is Agent C's live namespace — sample.ts was already the single constant that the editor, the server seed and the tests all import, so this is one source of truth without a mid-flight collision.
- **[2026-07-22 12:46]** Integration spike caught what unit tests could not: asked to rename a character, claude-sonnet-4.5 returns the document correct but WITHOUT the final newline. That diffs into a third hunk — 'delete a line break' — under the two real ones. The writer can't evaluate it and it teaches them the review list contains junk. Normalised in runEdit (matchTrailingNewline) and, independently, Agent B hit the same thing server-side and fixed it in checkRevision. Belt and braces on the manuscript path is fine.
- **[2026-07-22 12:46]** Seed swap broke two of Agent A's diff tests that keyed off the old placeholder prose. Rewrote them: the near-substring test now carries its OWN fixture rather than the shipping seed (a copy edit shouldn't silently delete coverage), and the multi-paragraph test asserts revised !== base so it can't pass against an unchanged document.
- **[2026-07-22 12:49]** Verified persistence keying at Tim's request: the server document is a fixed-key singleton. DOCUMENT_ID='default' is a constant in the frozen contracts, the file is document.json at a fixed path, readSnapshot()/writeSnapshot() take no id argument, and the client sends no identifier — no uuid/randomUUID/deviceId anywhere in api/ or src/lib/api/. Proven, not asserted: PUT a distinctive doc, then GET from a brand-new curl client (zero local state) returns it, and it survives a server process restart. REMAINING RISK is durability, not keying: the store is os.tmpdir(), which on Vercel is one warm instance's scratch disk. Real reinstall-survival needs KV/Blob provisioning.
- **[2026-07-22 12:55]** Agent C merged: WebView contenteditable editor over markdown (not the TextInput fallback) + doc store with debounced autosave. C shipped a web platform variant unprompted because react-native-webview has no web build and importing it kills the web bundle in exactly the silent way check-web.sh exists to catch — good call.
- **[2026-07-22 12:55]** REINSTALL GUARANTEE now pinned by tests (__tests__/doc-reinstall.test.ts), not just by reading code. Verified: persist key is the constant 'doc' (no device/install id); a fresh install with empty AsyncStorage takes the SERVER document; a stale-but-saved local copy LOSES to the server; the seed never beats a real server doc. The single exception is UNSAVED local work (dirty && origin!=='seed'), which wins and is immediately re-saved upstream — that is words whose save failed, not a cache, and dropping them would destroy the writer's work.
- **[2026-07-22 12:55]** C's warning caught a real bug in my screen: both editors are flex:1 and scroll internally, and I had nested MarkdownEditor inside <Screen scroll>. Restructured index.tsx into two layouts — writing mode gives the editor a bounded flex:1 box with controls pinned below, review mode scrolls the page because the change list is arbitrarily long.
- **[2026-07-22 13:04]** BUG: demo opened on the wrong document. Root cause was mine — I swapped the seed to James's passage and did NOT bump DOC_PERSIST_VERSION, the exact rule CLAUDE.md states. Devices that had opened the app kept the old sample in AsyncStorage; because unsaved local work correctly outranks the server, each device then pushed its stale copy UP and overwrote the new seed server-side (observed: v2 on the server was 'Survives' + the passage). Fixed by bumping to 2, resetting the store, adding npm run reset:doc, and pinning the rule with a test.
- **[2026-07-22 13:04]** Keyboard trap fixed: contenteditable had no way out. Added a Done button in the page toolbar (visible only while focused), a window.__blur bridge, an imperative blur() handle on MarkdownEditor honoured on BOTH platforms, and tap-outside-to-dismiss on the header and the controls card. Speaking now dismisses the keyboard first.
- **[2026-07-22 13:04]** Typing jank: the page called keepCaretVisible on visualViewport 'scroll', so every frame of a finger drag scrolled the writer back to the caret — the editor fighting the finger. Caret rescue now happens on 'resize' only (keyboard open/close), caret and toolbar work is coalesced to one rAF, and body height is only written when it actually changed. Added a test that new Function()s the inline script, because a syntax error there yields a WebView that renders and silently does nothing.
- **[2026-07-22 13:09]** Polish pass, styling only: flipped src/theme/tokens.ts to re-export src/theme/emend.ts, so the whole UI kit turned over in ONE move instead of surface by surface. Ivory #FBF7F0 page, deep ink #2B2620, champagne gold #C9A66B; Fraunces for prose and headings, Figtree for chrome, Literata removed and uninstalled. Diff kept its rose/sage but now reads them from tokens.diff rather than a pasted hex. Status bar to dark glyphs, editor page color-scheme to light, splash to ivory using the app icon as the mark. Verified with verify --full: iOS bundle, real-browser web check, shell gate. Bundle grep confirms zero occurrences of the old dark palette.
- **[2026-07-22 13:16]** Undo/history landed on the existing version stack. Key semantics, verified against the running server rather than assumed: each stack entry holds the markdown as it was BEFORE the edit named in its label, so restoring an entry undoes that edit and every control reads 'Undo <instruction>'. Restoring goes through doc.setMarkdown like any other edit, so it creates a NEW version and nothing is ever removed — undoing an undo is just another row.
- **[2026-07-22 13:16]** FINDING while verifying undo against the live server: the stack contained an unlabelled version with a ZERO-LENGTH document (v5, len=0) written from the device, plus a version containing text Tim had pasted while experimenting. An empty autosave overwriting a manuscript is a data-loss path. Undo rescues it, which is some consolation, but the write should not happen. Flagged to Tim rather than fixed blind under the clock.
- **[2026-07-22 13:20]** Editor branding reduced to near-nothing: the writing screen's header is now just the save status, quietly, right-aligned. The wordmark stays on the splash, the icon, the review screen and History. A wordmark above someone's manuscript is the app talking over the writer's words.
- **[2026-07-22 13:20]** Formatting chrome made contextual: the Bold/Italic/Title/Heading/Body bar is gone by default and appears only when there is a real non-collapsed selection inside the document. The Done control is gated on FOCUS rather than selection — requiring a selection to dismiss the keyboard would be the same trap with extra steps.
- **[2026-07-22 13:20]** Review surface ranked: 'Accept all N changes' is now full-width primary (most edits are accepted wholesale, so the loudest control should be honest about that), 'Reject all' dropped to ghost, and the per-change accept/reject rows kept under an explicit 'OR DECIDE ONE AT A TIME' label — ranked below, never buried, because deciding change by change IS the trust argument. Back and Discard both route through one confirm; a pending proposal is work that exists nowhere else.
- **[2026-07-22 13:23]** Added the frame above the working surface: the editor header now reads '‹ ALL CHAPTERS' on the left and 'CHAPTER 4 · SAVED' on the right, and /chapters is a real destination listing five chapters of an untitled novel. Only Chapter 4 opens; the other four render visibly inert — dimmed, no chevron, 'Not part of this prototype' — rather than tappable-and-empty. The back control names where it GOES, not where you are. Costs almost nothing and is the difference between a demo screen and software.
- **[2026-07-22 13:24]** P0 DATA-LOSS GUARD shipped. An empty document can no longer replace a non-empty manuscript unless the writer emptied it themselves. Two layers: the page refuses to emit anything before hydration completes (pageReady), and every change now carries userEdited — set only by typing, pasting or formatting, and RESET whenever the app replaces the document so a stale flag cannot lend a racy emit the writer's authority. The bridge drops exactly one case: empty, over content, not by the writer. Deliberately NOT 'reject empty' — a writer clearing their own chapter is legitimate and blocking it would be a worse bug. Root cause never fully reproduced; shipped anyway, because 'could not reproduce' is not a reason to leave a path that deletes someone's novel.
- **[2026-07-22 13:27]** P1 batch: Undo/History lifted to the top of the editor (reachable mid-sentence rather than filed beside the control for making NEW changes); review hierarchy re-ranked so Apply is the dominant action and Accept all / Reject all are lighter shortcuts sitting side by side — they only change what is selected, Apply is the only control that writes; voice moved to a floating circular button bottom-right carrying the icon's rose, closed by default so the editor is just the manuscript. The panel still force-opens for anything the writer MUST see (recording, thinking, errors), and the button hides while recording so nothing competes with Stop.
- **[2026-07-22 13:27]** Dictionary/story-bible deliberately NOT built: documented in the README as the next feature instead of stubbed. The server plumbing exists and was verified live (EditRequest.dictionary, injected by /api/edit), so what is missing is UI and persistence — and it should be a BOOK-level story bible beside the chapter index, not a per-chapter list, since a character's name is a fact about the book. No dead button.
- **[2026-07-22 13:34]** Story bible (dictionary) landed at BOOK level, on the chapters screen beside 'Untitled Novel' — a character's name is a fact about the novel, not about Chapter 4. Pre-seeded with Thomas/Susan/Janet/Maple/Vine/New York, persisted with a version+migrate, case-insensitive dedupe so 'susan' cannot sit beside 'Susan' and hand the model two spellings of one name.
- **[2026-07-22 13:34]** CAUGHT BEFORE SHIPPING: the flagship demo renames Susan to Janet, and BOTH are seeded story-bible terms — Agent B's prompt said 'never correct them' with no escape, which would have had the story bible silently block the headline demo. Added 'THE INSTRUCTION OVERRIDES THIS LIST' to the prompt and verified both directions live: with Susan and Janet both in the bible the rename still works (Janet 2, Susan 0), and a separate run still protects Wyckhampe/Aeryn Kestrelle while fixing trane->train and emty->empty.
- **[2026-07-22 13:39]** UNDO DIAGNOSED, not guessed. Store logic was provably fine (a repro test drove the exact button sequence against the real doc+history stores and it restored correctly). The break was that /api/versions returned count:0 — my own 'npm run reset:doc' during dictionary testing had wiped the stack, so undoTarget was null and the control never rendered. Root fragility fixed regardless: onUndo now REFRESHES the stack first and acts on what comes back, never on the background-refreshed cache, because a stale cache makes Undo a silent no-op that is indistinguishable from a dead button on a phone. No confirm added — undo is non-destructive in this model.
- **[2026-07-22 13:39]** Keyboard Done folded into the app's top bar beside Undo/History and removed from the page. The page now reports focus across the bridge so the bar knows when to offer it; the in-page rail is selection-gated formatting only. Tap-outside still dismisses; Done is the always-in-the-same-place backup.
- **[2026-07-22 13:39]** Dictionary renamed from 'Story bible' and promoted to its own screen at /dictionary, reached by a book icon on the far right of 'Untitled Novel'. It is a property of the book and a list that grows for the length of one, so it gets a page rather than a card crowding the chapter index.
- **[2026-07-22 13:39]** One back control everywhere: BackLink, top-left, chevron plus the DESTINATION name (‹ CHAPTER 4, ‹ UNTITLED NOVEL). Replaces the 'Back to …' buttons that sat at the bottom of scrolling pages — you should never have to scroll to leave a screen.
- **[2026-07-22 13:43]** Voice interaction rebuilt after a screenshot showed the problem: the panel said 'Tap to speak' as a heading AND 'Tap to speak an instruction' as its button — the same sentence twice — and the floating rose overlapped the gold button because an absolutely-positioned FAB and an in-flow panel were on screen together. There is no 'open the voice panel' any more. The button IS the control: HOLD to record and release to send (no panel at all, duration shown under the button), or TAP to start hands-free and let the status panel own Stop. A press only counts as a hold after 260ms so a slow tap is still a tap. The panel is status now — it appears only when there is something to see, and the button hides whenever it is up, so the two can never collide again.
- **[2026-07-22 13:45]** Review friction cut: the commit action is now a PINNED bar at the bottom of the review, never something you scroll past a long change list to find. With nothing decided it reads 'Apply all 2 changes' and does exactly that in one tap; once the writer has decided anything individually it becomes 'Apply N changes' and commits only those. Removed the separate Accept all / Reject all row — the fast path IS the primary button now.
- **[2026-07-22 13:45]** BUG CAUGHT while wiring apply-all: onApply read  from the render snapshot, so decideAll('accepted') followed by onApply() in the same tap would have applied an EMPTY decision map — the one-tap path would have silently done nothing. Now reads useProposal.getState(). Also dropped the diff preview's inner maxHeight ScrollView: the page scrolls now, and a nested scroller fights the outer one for the gesture.
- **[2026-07-22 13:46]** Hold-to-speak removed at Tim's call: a press-and-hold that starts recording is easy to trigger by accident while scrolling a manuscript, and it had never been verified on a device. One tested path beats two, one of which is a guess. Voice is now tap-to-start, tap-to-send on the same button, with the duration counting up under it — so the happy path never opens a panel at all. The panel is left for the two things a button cannot say: thinking, and failures.
- **[2026-07-22 13:46]** DEVLOG note that failed to write earlier (backticks ate it): onApply had been reading the review store from the RENDER SNAPSHOT, so decideAll then apply in one tap would have applied an empty decision map — the new one-tap Apply-all would have silently done nothing. It now reads useProposal.getState().
- **[2026-07-22 13:49]** Dictionary entries gained descriptions — without them it was a spelling list, not a dictionary, and 'Vine' alone cannot tell the model a street from a character. Store shape went from string[] to {term, description?}[], persist version bumped to 2 with a migrate. The frozen EditRequest.dictionary contract still carries string[], so a description rides along on its term as 'Vine — A street.', which is also how the model reads it best. Descriptions are editable in place; clearing one removes it rather than storing an empty string.
- **[2026-07-22 13:51]** Removed a genuinely duplicated commit block in the review: an earlier regex edit had left the old in-flow Apply/Discard card behind AND added the pinned bar, so 'Nothing accepted yet' and 'Discard this edit' were on screen twice. Caught because Tim saw it, not because a gate did — grep for the label count is now how I check that kind of edit.
- **[2026-07-22 13:51]** Voice status transitions smoothed. The jarring part was never the wording: 'Working out what you said' and 'Reading your manuscript' replaced each other with nothing constant between them, so each step read as a NEW screen rather than one request continuing. VoiceStatus keeps the rose on screen across recording -> transcribing -> thinking, breathing on a slow pulse (honest: it means still working, so a long edit does not look frozen), and only the words cross-fade underneath. A halo rather than a spinner — spinners say blocked.
- **[2026-07-22 13:51]** ConfirmDialog: the confirm now carries the emphasis colour, not cancel. Someone who opened the dialog came to do the thing; making them hunt for it in the quieter control is friction on a decision already made. The safety is the dialog existing at all, plus scrim-tap and hardware-back both meaning no.
- **[2026-07-22 13:53]** 'Undo Undo make it ominous' fixed: restoring an entry that is itself an undo is a REDO, not an undo of an undo. actionLabel alternates Undo/Redo forever so the label is always one of two words and never a stack of them, and a restore is SAVED under exactly the words the writer just pressed, so the history reads back as the list of things they actually did.
- **[2026-07-22 13:57]** JANETTE ROOT-CAUSED AND FIXED. The dictionary was only wired into /api/edit, never into transcription — so speech-to-text could hear 'Janet' and write 'Janette', and by the time the edit ran the instruction genuinely SAID Janette, which an explicit instruction is meant to outrank. We were guarding the wrong end of the pipe. /api/transcribe now takes the writer's proper nouns as a spelling hint. Reproduced the class of failure and proved the fix on identical audio: without the dictionary the recogniser wrote 'Catherine', with it 'Katharine'.
- **[2026-07-22 13:57]** README gained a 'what's next' note on the speak flow: it is tap-to-open, tap-to-start, tap-to-send because that is the path verified on a device; hold-to-speak was built and pulled for being untested and easy to trigger by accident while scrolling. The hybrid is probably right but needs real thumbs on real hardware, not a guess under a clock.
