REPLACE the placeholder magenta icon before delivery.
Name + art direction were decided — turn that into the real assets/images/icon.png.
Gate: npm run verify:shell (also runs in `npm run verify -- --full`).
